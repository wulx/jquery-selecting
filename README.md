## jquery-selecting -- a jquery plugin for chained selecting, coded by CoffeeScript 
### Usages:
```JavaScrit
$('selector').selecting({
  remote: {
    url: 'json_array_url',
    data: {}
  }
});
```
